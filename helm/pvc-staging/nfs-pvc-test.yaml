apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mlp-nfs-pvc-test
  namespace: mlp-explorer-test
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 10Ti
  volumeName: mlp-nfs-pvc-test