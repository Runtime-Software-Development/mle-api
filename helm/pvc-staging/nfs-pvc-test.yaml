apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mlp-test-nfs-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 10Ti
  volumeName: mlp-test-nfs-pv